# Test the configuration file
sudo nginx -t

# Show full config  also show the files it's reading
sudo nginx -T

# Show the version of Nginx

nginx -v  # Version only
nginx -V  # Version, modules and paths


# Show path for config file
nginx -t 2>&1 | grep file